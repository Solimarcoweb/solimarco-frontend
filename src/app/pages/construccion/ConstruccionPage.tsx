/**
 * Backwards-compatible alias: the construction template is now the data-driven
 * {@link ConstruccionLandingPage}. Kept so the `/construccion` route and any
 * existing imports keep working.
 */
export { default } from './ConstruccionLandingPage'
